let Conf,STORE_API_TOKEN;_662‍.x([["setToken",()=>setToken],["getToken",()=>getToken],["deleteToken",()=>deleteToken],["obj",()=>obj]]);_662‍.w("conf",[["default",["Conf"],function(v){Conf=v}]]);_662‍.w("./constants",[["STORE_API_TOKEN",["STORE_API_TOKEN"],function(v){STORE_API_TOKEN=v}]]);


// create a config instance
const store = new Conf();

/**
 * Saves an API token for a user
 * @param {string} token
 */
       function setToken(token) {
  store.set(STORE_API_TOKEN, token);
}

/**
 * Gets an API token for a user
 * @returns {string?} api token
 */
       function getToken() {
  const hello = () => "hello";
  return store.get(STORE_API_TOKEN, null);
}

/**
 * Remove an API token for a user
 * @returns {string?} api token
 */
       function deleteToken() {
  store.delete(STORE_API_TOKEN);
}

       const obj = {
  hello: () => "hello",
  goodbye: () => "goodbye",
};
