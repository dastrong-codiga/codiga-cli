let chalk;_edf‍.x([["printEmptyLine",()=>printEmptyLine],["printError",()=>printError],["printFailure",()=>printFailure],["printSuccess",()=>printSuccess],["printInfo",()=>printInfo],["printSuggestion",()=>printSuggestion],["printViolation",()=>printViolation],["printSubItem",()=>printSubItem]]);_edf‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);

/**
 * Used to space sections of output to improve readability
 */
       function printEmptyLine() {
  console.log("");
}

/**
 * Used to print an error code and message for errors that need more explanation
 * @param {string} text
 * @param {string} code
 */
       function printError(text, code) {
  _edf‍.g.console.error(`%s: ${text}`, chalk.red.bold(`ERROR (${code})`));
}

/**
 * Used to indicate something negative and needs the user's attention
 * @param {string} text
 */
       function printFailure(text) {
  _edf‍.g.console.log(chalk.red.bold(text));
}

/**
 * Used to indicate something was successful
 * @param {string} text
 */
       function printSuccess(text) {
  _edf‍.g.console.log(chalk.green.bold(text));
}

/**
 * Used to indicate some non-critical information
 * @param {string} text
 */
       function printInfo(text) {
  _edf‍.g.console.log(chalk.blue.bold(text));
}

/**
 * Used to indicate a follow-up action
 * @param {string} text
 * @param {string} command
 */
       function printSuggestion(text, command) {
  _edf‍.g.console.log(text, chalk.magenta(command));
}

/**
 * Used to print pertinent info violation for a Rosie violation
 * @param {number} index
 * @param {object} violation
 */
       function printViolation(index, violation) {
  _edf‍.g.console.log(
    chalk.red(
      `${index}) [${violation.filename}:${violation.start.line}:${violation.start.col}]`
    )
  );
  printSubItem(` ↳ ${violation.message}`);
}

/**
 * Used to print list item or some type of sub content
 * @param {string} text
 */
       function printSubItem(text) {
  _edf‍.g.console.log(chalk.italic(text));
}
